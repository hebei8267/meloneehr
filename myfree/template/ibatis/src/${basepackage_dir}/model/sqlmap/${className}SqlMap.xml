<#include "/macro.include"/>
<#assign className = table.className>   
<#assign classNameLower = className?uncap_first> 
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<!-- namespace必须指向Dao接口 -->
<mapper namespace="${basepackage}.dao.myBatis.${className}MyBatisDao">
	<!-- mybatis表结构与对象的映射关系 -->
	<resultMap id="${classNameLower}ResultMap" type="${basepackage}.entity.${className}">
		<#list table.columns as column>
        <result column="${column.sqlName}" property="${column.columnNameLower}" jdbcType=""/>
		</#list>
	</resultMap>
	
	<!-- 查询结果字段 -->
	<sql id="Base_Column_List" >
	<![CDATA[
    <#list table.columns as column>
    	${column.sqlName} <#if column_has_next>,</#if>
    </#list>
	]]>
  	</sql>
  	
  	<!-- 查询条件 -->
  	<sql id="Where_Clause" >
  		<where>
  		<#list table.columns as column>
  			<if test="${column.columnNameLower} != null and ${column.columnNameLower} !='' ">
  			${column.sqlName} = #{${column.columnNameLower}}
  			</if>
	    </#list>
	  	</where>
  	</sql>
  	
  	<!-- 取得${table.sqlName}信息(根据参数) -->
	<select id="get${className}List" parameterType="${basepackage}.entity.${className}"
		resultMap="${classNameLower}ResultMap">
		select
		<include refid="Base_Column_List" />
		from t_shop
		<include refid="Where_Clause" />
	</select>
</mapper>
